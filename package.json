{
  "name": "mfe-sandbox",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "predev": "pnpm --filter @repo/portless-mfe-dev build",
    "dev": "portless-mfe turbo run dev --ui=tui --filter=app --filter=www",
    "dev:apps": "pnpm dev",
    "predev:app": "pnpm --filter @repo/portless-mfe-dev build",
    "dev:app": "portless-mfe turbo run dev --filter=app",
    "predev:desktop": "pnpm --filter @repo/portless-mfe-dev build",
    "dev:desktop": "pnpm --filter desktop dev",
    "predev:www": "pnpm --filter @repo/portless-mfe-dev build",
    "dev:www": "portless-mfe turbo run dev --filter=www",
    "premfe:url": "pnpm --filter @repo/portless-mfe-dev build",
    "mfe:url": "portless-mfe url",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@repo/portless-mfe-dev": "workspace:*",
    "portless": "^0.12.0",
    "turbo": "~2.9.8",
    "typescript": "^5.0.0"
  }
}
