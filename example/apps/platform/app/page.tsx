export default function Page() {
	return (
		<main>
			<h1>@example/platform</h1>
			<p>
				Non-MFE Next.js app served via Portless. This fixture exists so the
				dev-proxy supervisor can be exercised with apps that are not part of
				the @vercel/microfrontends mesh.
			</p>
		</main>
	);
}
